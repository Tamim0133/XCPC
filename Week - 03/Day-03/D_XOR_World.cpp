#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long a, b;
    cin >> a >> b;

    bool a_odd = false;
    if (a & 1)
        a_odd = true;

    bool b_odd = false;
    if (b & 1)
        b_odd = true;

    return 0;
}